name: Dummy PR Status Check (for auto-merge)

on:
  workflow_run:
    workflows: ["Build, Push Docker Image, Update Helm Chart, Auto PR + Merge"]
    types:
      - completed

jobs:
  fake-check:
    runs-on: ubuntu-latest
    name: Build, Push, and Create PR
    steps:
      - name: Dummy PR status check
        run: echo "Auto status check passed"
